# ==========================================================================
   AQUARELA FOTOGRAFIA — CONFIGURAÇÃO CPANEL (.htaccess COMPATÍVEL)
   ==========================================================================

AddDefaultCharset UTF-8

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
</IfModule>
